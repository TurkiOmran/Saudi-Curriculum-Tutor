182

# تدريبي 2

ما وظيفة المقطع البرمجي التالي؟ وما مخرجاته؟

```python
import openpyxl
wb = openpyxl.load_workbook(filename="nutrition.xlsx")
sheet_obj = wb["arabic"]
for row in range (3,13):
value = float(sheet_obj.cell(row=row, column=5).value)
if (value &lt; 2):
print(sheet_obj.cell(row=row, column=1).value)
```

النتائج

---

# تدريبي 3

اكتب مقطعًا برمجيًا باستخدام مكتبة أوين بيكسل (openpyxl)، يعمل على تحميل مجموعة بيانات ملف التغذية و طباعة قيم عناوين الأعمدة.

---

# تدريبي 4

اكتب مقطعًا برمجيًا باستخدام مكتبة أوين بيكسل (openpyxl)، يعمل على تحميل مجموعة بيانات ملف التغذية و طباعة جميع القيم الغذائية الأصغر من 25.

وزارة التعليم
Ministry of Education
2025 - 1447