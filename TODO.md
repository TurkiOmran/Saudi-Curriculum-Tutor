# TODO

Out-of-scope items that surfaced while building other features. Each
entry names the symptom, root cause, and a couple of fix options so
whoever picks it up doesn't have to re-derive the analysis.

> The entries that lived here before `workflow-sandbox` merged in
> `f20595a` targeted the old multi-node pipeline (`src/graph/nodes/…`).
> Those nodes are gone; the bugs that were specific to them no longer
> apply. New TODOs against the tool-calling agent go below.
